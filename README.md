Hey gemini, give me a hello world function using typescript, make sure to wrap it in code blocks as we are inside a markdown file
```typescript

function helloWorld(): string {
  return "Hello, World!";
}

console.log(helloWorld());
```

